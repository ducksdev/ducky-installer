{
    "btcd": [
        {
            "url": "${BCH_RPC_HOST}:${BCH_RPC_PORT}",
            "auth": "${BCH_RPC_USER}",
            "pass": "${BCH_RPC_PASS}",
            "notify": true
        }
    ],
    "btcaddress": "${PAYOUT_ADDRESS}",
    "btcsig": "${POOL_SIG}",
    "serverurl": [
        "0.0.0.0:${STRATUM_PORT}"
    ],
    "mindiff": ${MINDIFF},
    "startdiff": ${STARTDIFF},
    "maxdiff": ${MAXDIFF},
    "logdir": "/var/log/ckpool",
    "zmqblock": "tcp://${BCH_RPC_HOST}:28334"
}
